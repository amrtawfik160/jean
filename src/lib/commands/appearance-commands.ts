import type { AppCommand } from './types'

// Light theme has been removed; the app is dark-only.
// This list is intentionally empty so the command registry stays consistent.
export const appearanceCommands: AppCommand[] = []
